# inventory-api
node js API to retrieve data from the Inventary system (POS)

###Enpoints
- GET /products
- GET /clients
- GET /products/most_sold
- GET /clients/:name
- POST /invoice/new
- POST /authenticate
- GET /logout
